var assert = require('assert');
var scope = require('../mini-angular');

describe('$digest', function(){
  describe('counter', function(){
    it('should be untouched if $digest was not run', function(){
      var scope = new Scope();
      assert.equal();
    })
  })
})
